#include "Presets.h"

// Preset data is defined inline in Presets.h (function-local static). This TU
// exists so the table has a single home in the build and to keep the header
// lightweight for the rest of the codebase.
